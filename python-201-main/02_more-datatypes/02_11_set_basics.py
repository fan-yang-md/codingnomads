# With the given sets `s` and `t`, demonstrate how you can:
# - Create an intersection of both sets
# - Create a union of both sets

s = {1, 2, 3, 4}
t = {3, 4, 5, 6}

intersect_set = s.intersection(t)
union_set = s.union(t)

print(intersect_set)
print(union_set)